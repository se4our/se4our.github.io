(function() {
    window.addEventListener('load', init);
    function init() {
      document.getElementById("generate").addEventListener("click", generate);
    }
    function generate() {
        const url = "";
    }
})();